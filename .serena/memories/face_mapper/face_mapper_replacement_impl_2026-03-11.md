Replaced the old heuristic-heavy FaceMapper editor flow with a thin import/setup layer. Added runtime ScriptableObject DevProject/Assets/Network_Game/ThirdPersonController/Scripts/PlayerFaceMaterialLibrary.cs to centralize base body material plus generated face materials. Updated DevProject/Assets/Network_Game/ThirdPersonController/Scripts/PlayerFaceMapper.cs to prefer the centralized library while preserving the legacy material array as a backward-compatible fallback. Rewrote DevProject/Assets/Network_Game/ThirdPersonController/Editor/FaceMapperEditorTool.cs to remove the Unity-side UV/photo bake path entirely; the editor tool now (1) syncs externally generated atlas PNGs from Assets/FacePhotos/Baked into FaceMat_* materials, (2) creates/updates PlayerFaceMaterialLibrary.asset, (3) auto-configures prefabs to point at the library, and (4) clears generated assets. Added a deprecated legacy menu entry that explicitly tells users the Unity bake path is removed and that Blender/Faceit plus external likeness baking is the supported flow. UnityMCP validate_script reported parser-like duplicate-method errors on the rewritten files even though direct file inspection showed no duplicates. A forced Unity asset refresh/compile removed the real compiler errors after the new file was imported; subsequent console reads no longer showed FaceMapper-related compile errors.