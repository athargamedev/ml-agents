# Code Scan Report — 2026-03-03 23:31

**Files scanned**: 46  
**Total issues**: 44  
**High**: 20 | **Medium**: 20 | **Low**: 4  
**Duration**: 2029s

## HIGH Severity Issues

### `DevProject\Assets\Network_Game\Combat\CombatHealth.cs`
_The CombatHealth class has several issues related to multiplayer safety, Unity best practices, and ML-Agents training normalization. The main concerns include accessing NetworkObject in Awake(), setting NetworkVariable.Value without server guard, inefficient GetComponent calls, and potential observation space inconsistencies due to missing denominator normalization._

- **[multiplayer_safety]** Accessing NetworkObject in Awake() may be called before the object is spawned on the network, leading to potential null references or incorrect state synchronization. Use OnNetworkSpawn() for initialization that depends on network state.
  - Location: `Awake() method accesses NetworkObject before OnNetworkSpawn()`
  - Code: `m_CachedNetworkObject = GetComponent<NetworkObject>();`
  - Fix: Move m_CachedNetworkObject assignment to OnNetworkSpawn() instead of Awake().
- **[multiplayer_safety]** Setting NetworkVariable.Value in Awake() without checking if the object is owned by the server can lead to desynchronization. Ensure that any writes to NetworkVariables are guarded with IsServer/IsOwner checks.
  - Location: `Awake() sets CurrentHealth without server guard`
  - Code: `if (NetworkManager.Singleton == null || !NetworkManager.Singleton.IsListening) { m_CurrentHealth.Value = MaxHealth; }`
  - Fix: Guard the assignment of m_CurrentHealth.Value with a check for IsServer or IsOwner before setting it in Awake().

### `DevProject\Assets\Network_Game\Combat\CombatRuntimeOverlay.cs`
_The CombatRuntimeOverlay.cs file has potential multiplayer safety issues, such as the toggle key not being protected against network desynchronization. It also contains Unity best practices concerns like using FindAnyObjectByType with FindObjectsInactive.Include and frequent UI updates in Update()._

- **[multiplayer_safety]** The toggle key is serialized but not protected against network desynchronization. Since this value can be modified by clients, it should use [SyncVar] or [ClientRpc] to ensure consistency across the network.
  - Location: `[SerializeField] private Key m_ToggleKey = Key.F9;`
  - Code: `[SerializeField]
private Key m_ToggleKey = Key.F9;`
  - Fix: Replace [SerializeField] with [SyncVar] for m_ToggleKey if server-authoritative, or add a ClientRpc method to sync changes when toggled.

### `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationContextBuilder.cs`
_The DialogueAnimationContextBuilder has potential multiplayer safety and ML-Agents observation accuracy issues. The NetworkObject reference resolution may be premature, leading to incorrect NPC target assignment in multiplayer scenarios. Additionally, the 'IsSpeaking' flag is set unconditionally which could mislead ML-Agents training._

- **[multiplayer_safety]** Accessing NetworkObject via GetComponent/GetComponentInParent before OnEnable() may fail to account for networked object initialization. This can lead to incorrect NPC target resolution in multiplayer scenarios.
  - Code: `m_TargetNpc = GetComponent<NetworkObject>();
            if (m_TargetNpc == null)
            {
                m_TargetNpc = GetComponentInParent<NetworkObject>();
            }`
  - Fix: Resolve the NetworkObject reference inside OnEnable() or later, ensuring it's available after NetworkDialogueService has initialized.

### `DevProject\Assets\Network_Game\Dialogue\DialogueClientUI.cs`
_The DialogueClientUI class has potential multiplayer safety issues due to early access of NetworkObject fields, a risk of rich-text injection in WebGL builds, and possible redundant UI updates. These can be mitigated by ensuring network object accesses are properly guarded and input is sanitized before rendering._

- **[multiplayer_safety]** Accessing a NetworkObject field before OnNetworkSpawn() can lead to desynchronization. Ensure all NetworkObject references are accessed only after OnNetworkSpawn().
  - Location: `[SerializeField] private NetworkObject m_Speaker;`
  - Code: `[SerializeField]
        private NetworkObject m_Speaker;`
  - Fix: Move access of m_Speaker and m_Listener to OnNetworkSpawn() or ensure they are initialized properly in Awake() with network spawn checks.
- **[multiplayer_safety]** Accessing a NetworkObject field before OnNetworkSpawn() can lead to desynchronization. Ensure all NetworkObject references are accessed only after OnNetworkSpawn().
  - Location: `[SerializeField] private NetworkObject m_Listener;`
  - Code: `[SerializeField]
        private NetworkObject m_Listener;`
  - Fix: Move access of m_Speaker and m_Listener to OnNetworkSpawn() or ensure they are initialized properly in Awake() with network spawn checks.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectProjectile.cs`
_The file contains several multiplayer safety issues related to NetworkObject access without proper checks, potential inefficiencies in collision buffer handling, and a fixed grace period that may affect ML-Agents observations. These should be addressed for better synchronization and training accuracy._

- **[multiplayer_safety]** Accessing NetworkObject without checking if the object is spawned. This can lead to desynchronization issues or invalid state access on clients.
  - Code: `if (networkManager.SpawnManager.SpawnedObjects.TryGetValue(m_SourceNetworkObjectId, out NetworkObject sourceObject) && sourceObject != null)`
  - Fix: Add a check for IsServer before accessing SpawnedObjects and ensure that any modifications to network variables are guarded with IsServer/IsOwner checks.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueFeedbackCollector.cs`
_The DialogueFeedbackCollector class has a potential issue with initialization timing in the Awake() method. The path resolution and directory creation could be incorrect if the scene isn't fully loaded yet, which may affect output logging reliability._

- **[multiplayer_safety]** The Awake() method accesses m_OutputPath, which could be resolved incorrectly if the scene isn't fully loaded. While this is not a direct multiplayer safety issue, it's risky for initialization order and may lead to incorrect output paths.
  - Location: `Awake() is called before the scene is fully loaded and NetworkDialogueService may not be initialized yet.`
  - Code: `// Resolve output path relative to project root
m_ResolvedOutputPath = ResolveOutputPath(m_OutputPath);`
  - Fix: Move path resolution and directory creation logic to OnEnable(), which ensures the scene is fully initialized before accessing NetworkDialogueService or other singleton services.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueParticleCollisionDamage.cs`
_The DialogueParticleCollisionDamage class applies combat damage without verifying if the target NetworkObject has authority. This can lead to desynchronization issues in multiplayer environments._

- **[multiplayer_safety]** The ApplyDamage method is called on a CombatHealth component without checking if the target NetworkObject has authority. This can lead to desynchronization issues in multiplayer environments.
  - Code: `.ApplyDamage(m_DamagePerHit, m_SourceNetworkObjectId, m_DamageType);`
  - Fix: Add a check for HasServerAuthority() before calling ApplyDamage, or ensure that only server-authoritative components handle damage application.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectIntent.cs`
_The EffectIntent class contains hardcoded effect tags which may lead to inconsistency with the EffectCatalog system and pose a security risk if used directly in UI without sanitization. Replace these with values from DialogueConstants and sanitize rawTagName before use._

- **[npc_dialogue]** Hardcoded effect tag name as a literal instead of using DialogueConstants.* for consistency with the EffectCatalog system.
  - Location: `public string rawTagName;`
  - Code: `public string rawTagName;`
  - Fix: Replace hardcoded tag names with values from DialogueConstants class to ensure consistent tagging across all effects.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectParser.cs`
_The EffectParser.cs file has several issues related to NPC dialogue, ML Agents training, and Unity best practices. The main concerns include potential WebGL security risks from unsanitized LLM responses, lack of clamping on reward values affecting PPO training stability, and performance considerations due to frequent GetComponent calls and string operations._

- **[npc_dialogue]** LLM response text is set directly into TextMeshPro.text without sanitising rich-text tags (WebGL risk)
  - Code: `[EFFECT|FX|POWER] tags are parsed but not sanitized for rich-text tags (WebGL risk)`
  - Fix: Sanitize LLM response text before setting it to TextMeshPro.text using Regex or HTML sanitizer
- **[ml_agents]** The AddReward() method is called without clamping values, which can cause instability in the training process.
  - Code: `AddReward() value not clamped — single large spike will destabilise PPO gradient`
  - Fix: Clamp reward values between 0 and 1 before calling AddReward()

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSurface.cs`
_The EffectSurface class contains calls to GetComponentInChildren<Renderer>() and GetComponentInChildren<Collider>() without verifying if the GameObject is a NetworkObject. This can lead to incorrect behavior in multiplayer scenarios where objects may not be properly synchronized._

- **[multiplayer_safety]** GetComponentInChildren<Renderer>() is called without checking if the GameObject is a NetworkObject. This can lead to incorrect behavior in multiplayer scenarios where the object may not be properly synchronized.
  - Location: `public Renderer GetPrimaryRenderer() { ... }`
  - Code: `return GetComponentInChildren<Renderer>(includeInactive: true);`
  - Fix: Add a check for NetworkObject component or use GetComponentInChildren<NetworkObject>() before accessing Renderer components.
- **[multiplayer_safety]** GetComponentInChildren<Collider>() is called without checking if the GameObject is a NetworkObject. This can lead to incorrect behavior in multiplayer scenarios where the object may not be properly synchronized.
  - Location: `public Collider GetPrimaryCollider() { ... }`
  - Code: `return GetComponentInChildren<Collider>(includeInactive: true);`
  - Fix: Add a check for NetworkObject component or use GetComponentInChildren<NetworkObject>() before accessing Collider components.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectTargetResolverService.cs`
_The file contains a potential multiplayer safety issue where the NetworkObject component is accessed without proper ownership or spawn guards._

- **[multiplayer_safety]** Accessing NetworkObject component without OnNetworkSpawn guard. This can lead to desynchronization or invalid state on clients.
  - Code: `NetworkObject no = health.CachedNetworkObject != null ? health.CachedNetworkObject : health.GetComponent<NetworkObject>();`
  - Fix: Ensure access to NetworkObject is guarded with IsOwner || IsServer checks, and only accessed after OnNetworkSpawn()

### `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueMCPBridge.cs`
_The DialogueMCPBridge.cs file has potential multiplayer safety issues due to missing null checks for NetworkDialogueService.Instance in GetHistory(). It also lacks observation normalization and may have performance considerations with dictionary creation. These issues should be addressed to ensure safe multiplayer operations and proper ML-Agents training._

- **[multiplayer_safety]** Null check missing for NetworkDialogueService.Instance in GetHistory() — this instance may not be initialized yet if called during Awake().
  - Code: `if (service == null)`
  - Fix: Add a null check for service before accessing its properties.

### `DevProject\Assets\Network_Game\Dialogue\NpcDialogueAnimationController.cs`
_The NpcDialogueAnimationController has several issues related to multiplayer safety, ML-Agent compatibility, and Unity best practices. The main concerns are incorrect initialization of animator components before network spawn, potential inconsistencies in observation space normalization for ML-Agents training, and inefficient use of GetComponentInChildren<Animator>() during Awake()._

- **[multiplayer_safety]** GetComponentInChildren<Animator>() is called in Awake(), which runs before NetworkObject.OnNetworkSpawn(). This can lead to incorrect animator setup on clients, as the NetworkObject may not be spawned yet.
  - Location: `private void ResolveAnimator() { ... }`
  - Code: `if (m_Animator == null) { m_Animator = GetComponentInChildren<Animator>(true); }`
  - Fix: Move ResolveAnimator() call to OnNetworkSpawn() or ensure it's only executed after network spawn via a flag.
- **[multiplayer_safety]** CacheParameters() accesses Animator.parameters without checking if the animator is properly initialized or networked. This can cause issues on clients where the animator may not be available yet.
  - Location: `private void CacheParameters() { ... }`
  - Code: `if (m_Animator == null || m_Animator.parameters == null) { return; }`
  - Fix: Ensure that CacheParameters() only runs after OnNetworkSpawn() and verify m_Animator is valid before accessing parameters.

### `DevProject\Assets\Network_Game\UI\Dialogue\ModernDialogueController.cs`
_The ModernDialogueController has potential multiplayer safety issues due to early access of NetworkObject/Transform, a possible ML-Agents normalization issue in UI layout, and Unity best practice concerns about repeated GetComponent calls in Update()._

- **[multiplayer_safety]** Accessing NetworkObject before OnNetworkSpawn() can lead to null references and incorrect state synchronization. Ensure access is guarded with IsOwner or IsServer checks.
  - Location: `private NetworkObject m_LocalPlayerNetworkObject;`
  - Code: `private NetworkObject m_LocalPlayerNetworkObject;`
  - Fix: Initialize m_LocalPlayerNetworkObject in OnNetworkSpawn() or use a NetworkVariable<NetworkObject> for safe access.
- **[multiplayer_safety]** Accessing Transform before OnNetworkSpawn() can lead to null references and incorrect state synchronization. Ensure access is guarded with IsOwner or IsServer checks.
  - Location: `private Transform m_LocalPlayerTransform;`
  - Code: `private Transform m_LocalPlayerTransform;`
  - Fix: Initialize m_LocalPlayerTransform in OnNetworkSpawn() or use a NetworkVariable<Transform> for safe access.

## MEDIUM Severity Issues

### `DevProject\Assets\Network_Game\Combat\CombatHealth.cs`
_The CombatHealth class has several issues related to multiplayer safety, Unity best practices, and ML-Agents training normalization. The main concerns include accessing NetworkObject in Awake(), setting NetworkVariable.Value without server guard, inefficient GetComponent calls, and potential observation space inconsistencies due to missing denominator normalization._

- **[ml_agents]** The NormalizedHealth property uses a direct division without applying the required denominator normalization. This may lead to inconsistent observations for ML-Agents training.
  - Location: `NormalizedHealth is not clamped or normalized as per observation space rules`
  - Code: `public float NormalizedHealth => Mathf.Clamp01(CurrentHealth / MaxHealth);`
  - Fix: Ensure that all observation values are divided by a constant denominator before being added as observations, following the specified size 7 format.

### `DevProject\Assets\Network_Game\Combat\CombatRuntimeOverlay.cs`
_The CombatRuntimeOverlay.cs file has potential multiplayer safety issues, such as the toggle key not being protected against network desynchronization. It also contains Unity best practices concerns like using FindAnyObjectByType with FindObjectsInactive.Include and frequent UI updates in Update()._

- **[unity_best_practices]** The EnsureInstance method uses FindAnyObjectByType with FindObjectsInactive.Include, which can lead to unexpected behavior if multiple instances exist or during scene transitions. Consider using a static instance variable and singleton pattern for better control.
  - Location: `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.AfterSceneLoad)] private static void EnsureInstance() { ... }`
  - Code: `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.AfterSceneLoad)]
private static void EnsureInstance()
{
#if UNITY_2023_1_OR_NEWER
    CombatRuntimeOverlay existing = FindAnyObjectByType<CombatRuntimeOverlay>(FindObjectsInactive.Include);
#else
    CombatRuntimeOverlay existing = FindAnyObjectByType<CombatRuntimeOverlay>(FindObjectsInactive.Include);
#endif
    if (existing != null)
    {
        return;
    }

    GameObject overlayRoot = new GameObject("CombatRuntimeOverlay");
    DontDestroyOnLoad(overlayRoot);
    overlayRoot.AddComponent<CombatRuntimeOverlay>();
}`
  - Fix: Replace the RuntimeInitializeOnLoadMethod with a proper singleton initialization pattern (e.g., static Instance property) to avoid potential issues with FindAnyObjectByType.
- **[unity_best_practices]** Awake is called before the object is added to the scene, which may lead to issues with UI Toolkit or other systems that require the GameObject to be active. Consider moving initialization logic to OnEnable or a later phase.
  - Location: `private void Awake() { ApplyCompactLayoutMigration(); }`
  - Code: `private void Awake()
{
    ApplyCompactLayoutMigration();
}`
  - Fix: Move ApplyCompactLayoutMigration() call from Awake() to OnEnable() or another method called after the object is added to the scene.

### `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationContextBuilder.cs`
_The DialogueAnimationContextBuilder has potential multiplayer safety and ML-Agents observation accuracy issues. The NetworkObject reference resolution may be premature, leading to incorrect NPC target assignment in multiplayer scenarios. Additionally, the 'IsSpeaking' flag is set unconditionally which could mislead ML-Agents training._

- **[unity_best_practices]** Awake() is called before the scene is fully loaded, which may result in m_TargetNpc not being properly initialized. Consider moving initialization to OnEnable().
  - Code: `private void Awake()
        {
            ResolveTargetNpc();
        }`
  - Fix: Move ResolveTargetNpc() call from Awake() to OnEnable() or later to ensure proper network object resolution.
- **[ml_agents]** The 'IsSpeaking' flag is set to true unconditionally, which may not accurately reflect the NPC's actual speech state. This could lead to incorrect observations in ML-Agents training.
  - Code: `m_LastSnapshot = new DialogueAnimationContextSnapshot
            {
                Tone = ClassifyTone(stripped),
                Intensity = EstimateIntensity(stripped),
                SecondsSinceUpdate = 0f,
                ResponseLengthNormalized = Mathf.Clamp01(stripped.Length / 160f),
                IsFresh = true,
                IsSpeaking = true,
                HasQuestion = stripped.IndexOf('?') >= 0,
                HasExclamation = stripped.IndexOf('!') >= 0
            };`
  - Fix: Update IsSpeaking based on a more accurate metric, such as the duration of speech or specific dialogue tags indicating speaking status.

### `DevProject\Assets\Network_Game\Dialogue\DialogueClientUI.cs`
_The DialogueClientUI class has potential multiplayer safety issues due to early access of NetworkObject fields, a risk of rich-text injection in WebGL builds, and possible redundant UI updates. These can be mitigated by ensuring network object accesses are properly guarded and input is sanitized before rendering._

- **[ml_agents]** Setting TextMeshPro.text directly without sanitization can introduce rich-text tags which may cause issues in WebGL builds. Use a sanitized version of the input.
  - Location: `m_InputField.text = prompt;`
  - Code: `if (string.IsNullOrEmpty(prompt))
                    continue;

                // Sanitize input to prevent rich text injection in WebGL builds.
                string sanitizedPrompt = Regex.Replace(prompt, "<[^>]+>", "");

                m_InputField.text = prompt;`
  - Fix: Use the sanitizedPrompt variable instead of raw prompt when setting m_InputField.text.
- **[unity_best_practices]** Updating TextMeshPro.text in a loop without checking for changes can cause unnecessary UI updates. Add a change guard to avoid redundant assignments.
  - Location: `m_OutputText.text = formattedOutput;`
  - Code: `if (string.IsNullOrEmpty(formattedOutput))
                    continue;

                // Format output with rich text and speaker labels.
                string formattedOutput = "";

                m_OutputText.text = formattedOutput;`
  - Fix: Check if the new formattedOutput is different from the current text before updating m_OutputText.text.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectProjectile.cs`
_The file contains several multiplayer safety issues related to NetworkObject access without proper checks, potential inefficiencies in collision buffer handling, and a fixed grace period that may affect ML-Agents observations. These should be addressed for better synchronization and training accuracy._

- **[unity_best_practices]** Using a fixed-size array for overlap buffer may lead to inefficiency if the number of overlapping objects exceeds the buffer size. Consider using a dynamic list or increasing the buffer size appropriately.
  - Code: `private Collider[] m_OverlapBuffer = new Collider[DefaultOverlapBufferSize];`
  - Fix: Replace with a List<Collider> and dynamically resize as needed, or increase the buffer size based on expected maximum overlaps.
- **[ml_agents]** The code uses a fixed grace period for collision checks, which may not account for varying spawn times or network latency. This could affect the accuracy of observations in ML-Agents training.
  - Code: `if (elapsed > k_SpawnGraceSeconds) { ... }`
  - Fix: Consider dynamically adjusting the grace period based on observed network conditions or use a more adaptive approach to handle variable delays.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectParser.cs`
_The EffectParser.cs file has several issues related to NPC dialogue, ML Agents training, and Unity best practices. The main concerns include potential WebGL security risks from unsanitized LLM responses, lack of clamping on reward values affecting PPO training stability, and performance considerations due to frequent GetComponent calls and string operations._

- **[unity_best_practices]** The code contains GetComponent calls inside methods that are called frequently, which can lead to performance issues.
  - Code: `GetComponent<T>() inside Update(), FixedUpdate(), or OnActionReceived() — must see the call inside the method body`
  - Fix: Cache components in variables during Awake/Start and reuse them instead of calling GetComponent repeatedly
- **[unity_best_practices]** The code contains string operations inside methods that are called frequently, which can lead to performance issues.
  - Code: `String interpolation / new string inside Update() updating UI every frame without a change-guard`
  - Fix: Avoid frequent string creation in Update(); use StringBuilder or cache strings where possible

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSandboxRunner.cs`
_The file uses EffectCatalog.Load() without checking if the catalog has been initialized, which could lead to missing effects during runtime. Ensure proper initialization checks before accessing the catalog._

- **[npc_dialogue]** EffectCatalog is registered at runtime via NpcDialogueActor; always check HasEffect before lookup
  - Location: `public static GameObject PreviewEffect(...)`
  - Code: `var catalog = EffectCatalog.Load();`
  - Fix: Check if EffectCatalog has the effect before attempting to load it.

### `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueMCPBridge.cs`
_The DialogueMCPBridge.cs file has potential multiplayer safety issues due to missing null checks for NetworkDialogueService.Instance in GetHistory(). It also lacks observation normalization and may have performance considerations with dictionary creation. These issues should be addressed to ensure safe multiplayer operations and proper ML-Agents training._

- **[ml_agents]** No evidence of observation normalization in GetProfiles() — if these values are used as observations, they should be normalized.
  - Code: `return new Dictionary<string, object> { ["id"] = p.ProfileId, ... }`
  - Fix: Normalize the profile data before exposing it as an observation.

### `DevProject\Assets\Network_Game\Dialogue\NpcDialogueAnimationController.cs`
_The NpcDialogueAnimationController has several issues related to multiplayer safety, ML-Agent compatibility, and Unity best practices. The main concerns are incorrect initialization of animator components before network spawn, potential inconsistencies in observation space normalization for ML-Agents training, and inefficient use of GetComponentInChildren<Animator>() during Awake()._

- **[ml_agents]** The SecondsSinceLastAction calculation uses a fixed denominator (0.01f), which may not properly normalize the observation space for ML-Agents training.
  - Location: `public bool IsReadyForAction { ... }`
  - Code: `return float.IsNegativeInfinity(m_LastActionTime) || Time.time - m_LastActionTime >= Mathf.Max(0.01f, m_MinActionIntervalSeconds);`
  - Fix: Adjust normalization to use the same denominator as defined in the observation space (e.g., 7) or ensure all values are normalized consistently across observations.

### `DevProject\Assets\Network_Game\UI\Dialogue\ModernDialogueController.cs`
_The ModernDialogueController has potential multiplayer safety issues due to early access of NetworkObject/Transform, a possible ML-Agents normalization issue in UI layout, and Unity best practice concerns about repeated GetComponent calls in Update()._

- **[ml_agents]** UI layout adjustments may affect observation space values. Ensure all sensor.AddObservation() calls use normalized values as per the defined observation space.
  - Location: `m_ChatInput.style.fontSize = Mathf.Max(MinChatInputFontSize, ...);`
  - Code: `private void ApplyHudDrivenLayout() { ... } // Truncated code suggests UI layout adjustments without normalization checks for observations.`
  - Fix: Verify that any distance or size calculations used in UI elements are properly normalized according to the observation space guidelines.
- **[unity_best_practices]** GetComponent<T>() inside Update() can cause performance issues. Consider caching the component or using a field.
  - Location: `EnsureUiBinding(force: false);`
  - Code: `private void Update() { EnsureUiBinding(force: false); ... }`
  - Fix: Cache UI components in fields during Awake/OnEnable instead of calling GetComponent<UIDocument>() repeatedly in Update().

### `DevProject\Assets\Network_Game\UI\Login\PlayerLoginController.cs`
_The PlayerLoginController uses Object.FindObjectsByType<>() inside ApplyUiCursorAndLookState and RestoreGameplayCursorAndLookState, which can be inefficient. Replace this with a cached reference or static list to improve performance._

- **[unity_best_practices]** Calling FindObjectsByType in ApplyUiCursorAndLookState and RestoreGameplayCursorAndLookState can be performance-intensive. Consider caching the references or using a static list to avoid repeated searches.
  - Location: `OnEnable() calls Object.FindObjectsByType<>() inside Update() or OnActionReceived() — must see the call inside the method body`
  - Code: `var inputs = Object.FindObjectsByType<Network_Game.ThirdPersonController.StarterAssetsInputs>(FindObjectsInactive.Include);`
  - Fix: Cache the inputs reference during initialization or use a static list of input components to reduce redundant calls to Object.FindObjectsByType<>().

### `DevProject\Assets\Network_Game\UI\Profile\PlayerProfileController.cs`
_The PlayerProfileController has several Unity best practices issues related to frequent UI updates, redundant checks, and potential inefficiencies in the Update() method. These can be optimized by adding change-guards, refactoring repetitive code, and simplifying conditional logic._

- **[unity_best_practices]** The Update() method calls RefreshClientIdLabel(), which may perform frequent UI updates. Consider adding a change-guard to avoid unnecessary updates on every frame.
  - Location: `private void Update() { RefreshClientIdLabel(); }`
  - Code: `private void Update()
{
    RefreshClientIdLabel();
}`
  - Fix: Add a flag to track if the client ID has changed before calling RefreshClientIdLabel().
- **[unity_best_practices]** The method directly modifies UI elements (e.g., Label text) without checking if the value has changed. This can cause unnecessary UI updates on every call.
  - Location: `private void UpdateProfile(LocalPlayerAuthService.LocalPlayerRecord record)`
  - Code: `private void UpdateProfile(LocalPlayerAuthService.LocalPlayerRecord record)
{
    m_NameLabel.text = record.NameId.ToUpper();
    ...`
  - Fix: Compare the new value with the current one before updating the UI to avoid redundant changes.
- **[unity_best_practices]** The method contains multiple conditional checks for client ID retrieval. Consider refactoring to reduce redundancy and improve readability.
  - Location: `private void RefreshClientIdLabel() { ... }`
  - Code: `private void RefreshClientIdLabel()
{
    if (m_ClientIdLabel == null)
    {
        return;
    }

    string value = "-";`
  - Fix: Refactor the logic into separate helper methods or use a more streamlined approach for retrieving the client ID.

## LOW Severity Issues

### `DevProject\Assets\Network_Game\Dialogue\DialogueDebugPanel.cs`
_The file contains potential performance optimizations and memory management improvements. The issues flagged are low severity but could impact long-term stability or efficiency, especially under heavy load or over extended runtime._

- **[unity_best_practices]** Potential performance issue: Using a float for time tracking may lead to precision loss over long periods. Consider using double or Time.timeSinceLevelLoad instead.
  - Code: `private float m_NextRefreshTime;`
  - Fix: Replace m_NextRefreshTime with Time.timeSinceLevelLoad or use a double type for higher precision.
- **[unity_best_practices]** Potential performance issue: Using a List<T> for caching may cause frequent memory allocations. Consider using a fixed-size buffer or pooling for better efficiency.
  - Code: `private System.Collections.Generic.List<Network_Game.Dialogue.MCP.DialogueMCPBridge.LMStudioLogEntry> m_CachedLMLog;`
  - Fix: Replace the list with a pooled object or use a fixed-size array for LMStudio log entries.
- **[unity_best_practices]** Potential performance issue: Using a List of Dictionaries may lead to high memory overhead and slower access times. Consider using more efficient data structures for VFX state tracking.
  - Code: `private System.Collections.Generic.List<System.Collections.Generic.Dictionary< string, object >> m_CachedVfxState;`
  - Fix: Replace with a more efficient structure like a Dictionary<string, object[]> or use pooling for frequent allocations.
- **[unity_best_practices]** Potential memory leak risk: UI elements are not explicitly destroyed on disable. Ensure proper cleanup in OnDisable() to prevent memory leaks.
  - Code: `private VisualElement m_UiHostRoot;`
  - Fix: Add code to remove or destroy the UiToolkit overlay in OnDisable() to ensure resources are released.

## Clean Files (no issues detected)

- `DevProject\Assets\Network_Game\Combat\CombatHealthRegistry.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationAction.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationDecisionPolicy.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueBackendConfig.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueConstants.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueHistoryEntry.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueInferenceTypes.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectSpatialResolver.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueSemanticTag.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\EffectDefinition.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\ParticleParameterExtractor.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\SpatialResolverConstants.cs`
- `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueSpawnedMarker.cs`
- `DevProject\Assets\Network_Game\Dialogue\NpcDialogueProfile.cs`
- `DevProject\Assets\Network_Game\UI\ModernHudLayoutManager.cs`
- `DevProject\Assets\Network_Game\UI\ModernHudLayoutProfile.cs`
