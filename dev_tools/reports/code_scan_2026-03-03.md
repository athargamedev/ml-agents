# Code Scan Report — 2026-03-03 03:36

**Files scanned**: 42  
**Total issues**: 38  
**High**: 12 | **Medium**: 21 | **Low**: 5  
**Duration**: 2703s

## HIGH Severity Issues

### `DevProject\Assets\Network_Game\Combat\CombatHealth.cs`
_The file has multiple multiplayer safety issues related to premature access of NetworkObject and unsafe health initialization, along with potential Unity best practice improvements regarding string comparison in dictionaries._

- **[multiplayer_safety]** Accessing NetworkObject component in Awake() before OnNetworkSpawn() may cause issues if the object hasn't been spawned yet. Safe to access only after OnEnable() or OnNetworkSpawn().
  - Code: `m_CachedNetworkObject = GetComponent<NetworkObject>();`
  - Fix: Move NetworkObject assignment to OnNetworkSpawn() or later, ensuring it's only accessed after the network spawn is complete.
- **[multiplayer_safety]** Setting CurrentHealth in Awake() may not be safe if the network isn't ready. Prefer setting it in OnEnable() or later after ensuring the scene is fully initialized.
  - Code: `if (NetworkManager.Singleton == null || !NetworkManager.Singleton.IsListening) { m_CurrentHealth.Value = MaxHealth; }`
  - Fix: Move health initialization to OnEnable() or ensure that NetworkManager.Singleton is checked at a safer time (e.g., during runtime).

### `DevProject\Assets\Network_Game\Dialogue\DialogueClientUI.cs`
_The DialogueClientUI.cs file has potential multiplayer safety issues due to early access of NetworkObject fields, a possible ML-Agents training instability risk from direct text assignment without normalization, and Unity best practice concerns with input field listener attachment._

- **[multiplayer_safety]** NetworkObject field accessed in Awake() without OnNetworkSpawn guard. Accessing NetworkObject before OnNetworkSpawn can lead to desynchronization issues.
  - Code: `[SerializeField] private NetworkObject m_Speaker;`
  - Fix: Ensure access to m_Speaker and m_Listener is guarded with OnNetworkSpawn or use a flag to delay initialization until the object is properly networked.
- **[multiplayer_safety]** NetworkObject field accessed in Awake() without OnNetworkSpawn guard. Accessing NetworkObject before OnNetworkSpawn can lead to desynchronization issues.
  - Code: `[SerializeField] private NetworkObject m_Listener;`
  - Fix: Ensure access to m_Speaker and m_Listener is guarded with OnNetworkSpawn or use a flag to delay initialization until the object is properly networked.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectProjectile.cs`
_The file contains several critical issues related to multiplayer safety, ML-Agent training stability, and Unity best practices. The main concerns are accessing network resources without server checks, potential observation normalization issues, and inefficient array usage in collision detection._

- **[multiplayer_safety]** Accessing NetworkManager.Singleton without checking if it's the server. This can lead to desynchronization issues in multiplayer scenarios.
  - Code: `if (networkManager.SpawnManager.SpawnedObjects.TryGetValue(m_SourceNetworkObjectId, out NetworkObject sourceObject) && sourceObject != null)`
  - Fix: Add a check for IsServer before accessing NetworkManager.Singleton.SpawnManager

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueFeedbackCollector.cs`
_The DialogueFeedbackCollector may initialize before the NetworkDialogueService, which could lead to dependency issues. Move initializations dependent on it to OnEnable() or later._

- **[multiplayer_safety]** The Awake() method may be called before the NetworkDialogueService has been initialized, which could lead to issues if any dependencies on it are used. The NetworkFeedbackCollector should ensure that NetworkDialogueService is available before performing operations that depend on it.
  - Location: `Awake() method called before NetworkDialogueService is initialized`
  - Code: `// Resolve output path relative to project root
            m_ResolvedOutputPath = ResolveOutputPath(m_OutputPath);`
  - Fix: Move initialization logic that depends on NetworkDialogueService to OnEnable() or later in the Unity lifecycle to guarantee it's initialized.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueParticleCollisionDamage.cs`
_The DialogueParticleCollisionDamage class has potential multiplayer desynchronization risks due to missing ownership checks when applying damage. Additionally, the damage value is not clamped which could affect ML-Agents training quality. Collection initializations are also recommended to be moved to constructor for better practices._

- **[multiplayer_safety]** The method ApplyDamage is called on a NetworkObject's CombatHealth component without checking if the target has ownership. This can lead to desynchronization issues in multiplayer environments.
  - Code: `.ApplyDamage(m_DamagePerHit, m_SourceNetworkObjectId, m_DamageType);`
  - Fix: Add a check for owner authority before calling ApplyDamage on remote objects.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectTargetResolverService.cs`
_The file contains a potential multiplayer safety issue where the NetworkObject component is accessed via GetComponent<NetworkObject>() without ensuring that the object has been spawned (OnNetworkSpawn). This can lead to desynchronization or missing components on clients. The code should be moved inside OnNetworkSpawn() for NetworkBehaviours, or use NetworkVariable.Value with proper ownership checks before accessing NetworkObject._

- **[multiplayer_safety]** Accessing NetworkObject component via GetComponent<NetworkObject>() without checking if the object is spawned (OnNetworkSpawn) in a multiplayer context. This can lead to desynchronization or missing components on clients.
  - Code: `NetworkObject no = health.CachedNetworkObject != null ? health.CachedNetworkObject : health.GetComponent<NetworkObject>();`
  - Fix: Ensure that this code runs inside OnNetworkSpawn() for NetworkBehaviours, or use NetworkVariable.Value with proper ownership checks before accessing NetworkObject.

### `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueMCPBridge.cs`
_The file contains potential multiplayer safety issues due to early access of NetworkDialogueService.Instance. Ensure this is accessed only after OnEnable() for safe initialization._

- **[multiplayer_safety]** Access to NetworkDialogueService.Instance in Awake() or before OnEnable() can lead to race conditions. Ensure this is accessed after OnEnable() for safe initialization.
  - Code: `if (service == null)`
  - Fix: Check if the code accesses NetworkDialogueService.Instance in Awake() or before OnEnable(). If so, move the access to after OnEnable() or use a flag to delay execution until service is initialized.

### `DevProject\Assets\Network_Game\Dialogue\MCP\MCPShowcasePolishController.cs`
_The MCPShowcasePolishController has potential multiplayer safety issues with non-Netcode-compliant fields and possible performance overhead from frequent EnsureSetup() calls. Review serialized fields to ensure they are NetworkObjects or accessed only after OnNetworkSpawn(). Optimize EnsureSetup() usage in frequently called methods._

- **[multiplayer_safety]** CinemachineVirtualCameraBase is not a NetworkObject and should be accessed only after OnNetworkSpawn() to ensure it's properly initialized on the client. Accessing it in Awake() or Start() could lead to desynchronization issues.
  - Location: `[SerializeField] private CinemachineVirtualCameraBase m_ShowcaseVirtualCamera;`
  - Code: `[SerializeField]
        private CinemachineVirtualCameraBase m_ShowcaseVirtualCamera;`
  - Fix: Ensure that any access to m_ShowcaseVirtualCamera happens within OnNetworkSpawn() or later, and avoid using it before the NetworkObject is fully spawned.
- **[multiplayer_safety]** Transform is not a NetworkObject and should be accessed only after OnNetworkSpawn() to ensure it's properly initialized on the client. Accessing it in Awake() or Start() could lead to desynchronization issues.
  - Location: `[SerializeField] private Transform m_CameraAnchorsRoot;`
  - Code: `[SerializeField]
        private Transform m_CameraAnch,Root;`
  - Fix: Ensure that any access to m_CameraAnchorsRoot happens within OnNetworkSpawn() or later, and avoid using it before the NetworkObject is fully spawned.

### `DevProject\Assets\Network_Game\UI\Dialogue\ModernDialogueController.cs`
_The ModernDialogueController has potential multiplayer safety, ML-Agents training efficiency, Unity best practices, and NPC dialogue pipeline issues. Key fixes include ensuring network object access is delayed until OnNetworkSpawn(), increasing proximity check intervals for better performance, adding null checks for Transform fields, and verifying effect presence before accessing them._

- **[multiplayer_safety]** Accessing a NetworkObject field before OnNetworkSpawn() can lead to desynchronization issues. Ensure all networked object references are accessed only after OnNetworkSpawn().
  - Location: `private NetworkObject m_SelectedNpc;`
  - Code: `private NetworkObject m_LocalPlayerNetworkObject;`
  - Fix: Move access of m_LocalPlayerNetworkObject and m_SelectedNpc to OnNetworkSpawn() or later.

## MEDIUM Severity Issues

### `DevProject\Assets\Network_Game\Combat\CombatHealth.cs`
_The file has multiple multiplayer safety issues related to premature access of NetworkObject and unsafe health initialization, along with potential Unity best practice improvements regarding string comparison in dictionaries._

- **[unity_best_practices]** Using StringComparer.Ordinal for dictionary keys may lead to unexpected behavior if the animator parameters are case-insensitive or use different casing. Consider using StringComparer.OrdinalIgnoreCase for consistency.
  - Code: `private readonly Dictionary<string, AnimatorControllerParameterType> m_AnimatorParameters = new Dictionary<string, AnimatorControllerParameterType>(StringComparer.Ordinal);`
  - Fix: Change the string comparer to StringComparer.OrdinalIgnoreCase for better compatibility with animator parameter names.
- **[unity_best_practices]** Same as above; using StringComparer.Ordinal may cause issues with case sensitivity in coroutine keys. Use StringComparer.OrdinalIgnoreCase for consistency.
  - Code: `private readonly Dictionary<string, Coroutine> m_AnimatorBoolPulseCoroutines = new Dictionary<string, Coroutine>(StringComparer.Ordinal);`
  - Fix: Change the string comparer to StringComparer.OrdinalIgnoreCase for better compatibility and reliability.

### `DevProject\Assets\Network_Game\Dialogue\DialogueClientUI.cs`
_The DialogueClientUI.cs file has potential multiplayer safety issues due to early access of NetworkObject fields, a possible ML-Agents training instability risk from direct text assignment without normalization, and Unity best practice concerns with input field listener attachment._

- **[ml_agents]** Directly setting TextMeshPro text without normalization. This may include raw world-space values or unnormalized observations, which can destabilize ML-Agents training.
  - Code: `m_OutputText.text = new string(text.ToCharArray().Take(m_MaxMessageCharacters).ToArray());`
  - Fix: Normalize the text length and ensure it's clamped to a fixed range before assigning to m_OutputText.text.
- **[unity_best_practices]** Using onEndEdit listener in Awake() may cause issues if the input field is not yet active. Consider using a flag or checking for null before adding listeners.
  - Code: `m_InputField.onEndEdit.AddListener(OnInputFieldEndEdit);`
  - Fix: Add a null check for m_InputField before attaching the listener to prevent potential crashes during initialization.

### `DevProject\Assets\Network_Game\Dialogue\DialogueDebugPanel.cs`
_The DialogueDebugPanel.cs file uses deprecated OnGUI() for rendering, performs heavy operations inside Update(), and may have performance issues due to frequent UI updates. Refactoring to use native UI elements and optimizing update logic can improve maintainability and performance._

- **[unity_best_practices]** GetComponent<T>() inside Update() or similar hot methods. The code uses OnGUI(), which is also a performance concern, but the main issue here is that UI updates are happening in Update(). Consider using Unity's built-in UI system (e.g., Canvas + TextMeshPro) for better performance and maintainability.
  - Code: `private void Update() { TryEnsureUiToolkitOverlay(); ... }`
  - Fix: Refactor to use Unity's native UI elements instead of OnGUI() for rendering debug panels. Avoid performing heavy operations inside Update() by using coroutines or event-based updates.
- **[unity_best_practices]** Using Keyboard.current inside Update() can be performance-intensive. Consider using InputSystem's low-level methods or caching the key state to reduce overhead.
  - Code: `private void Update() { ... if (Keyboard.current != null && Keyboard.current[m_ToggleKey].wasPressedThisFrame) { m_ShowPanel = !m_ShowPanel; } ... }`
  - Fix: Replace Keyboard.current checks with cached input states or use Input.GetKeyDown() for better performance in hot paths like Update().
- **[unity_best_practices]** OnGUI() is deprecated and can cause rendering issues, especially in Unity 6. Consider migrating to the new UI system (e.g., Canvas, TextMeshPro) for better compatibility and performance.
  - Code: `private void OnGUI() { ... }`
  - Fix: Migrate debug panel rendering from OnGUI() to a native UI system using Canvas and TextMeshPro components for improved maintainability and performance.
- **[unity_best_practices]** Performing heavy operations inside Update() can lead to performance issues. Consider using a coroutine or event-based system for periodic updates.
  - Code: `private void Update() { ... if (m_ShowPanel && Application.isPlaying && Time.realtimeSinceStartup >= m_NextRefreshTime) { ... } ... }`
  - Fix: Refactor the refresh logic into a separate coroutine that runs at intervals, reducing CPU load during each frame update.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectProjectile.cs`
_The file contains several critical issues related to multiplayer safety, ML-Agent training stability, and Unity best practices. The main concerns are accessing network resources without server checks, potential observation normalization issues, and inefficient array usage in collision detection._

- **[ml_agents]** The speed value is clamped but not normalized for the observation space. This could affect reward shaping and training stability.
  - Code: `m_Speed = Mathf.Max(0.1f, speed);`
  - Fix: Normalize the speed value using a constant denominator before storing it.
- **[unity_best_practices]** Using a fixed-size array for overlap buffer may not be efficient. Consider using a dynamic list or resizing based on demand.
  - Code: `private Collider[] m_OverlapBuffer = new Collider[DefaultOverlapBufferSize];`
  - Fix: Replace with List<Collider> and resize dynamically as needed.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueParticleCollisionDamage.cs`
_The DialogueParticleCollisionDamage class has potential multiplayer desynchronization risks due to missing ownership checks when applying damage. Additionally, the damage value is not clamped which could affect ML-Agents training quality. Collection initializations are also recommended to be moved to constructor for better practices._

- **[unity_best_practices]** The dictionary is initialized in a field declaration. It's better to initialize it in the constructor or during setup for clarity and memory management.
  - Code: `private readonly Dictionary<EntityId, float> m_NextHitAtByTarget = new Dictionary<EntityId, float>();`
  - Fix: Initialize the dictionary in the constructor or during initialization methods.
- **[unity_best_practices]** The collider array is initialized in a field declaration. It's better to initialize it in the constructor or during setup for clarity and memory management.
  - Code: `private Collider[] m_OverlapBuffer = new Collider[DefaultOverlapBufferSize];`
  - Fix: Initialize the collider array in the constructor or during initialization methods.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectParser.cs`
_The EffectParser.cs file contains several unresolved method references which could lead to compilation or runtime issues. The methods SplitParts, TrySplitKeyValue, TryResolveDefinition, and the logger NGLog are not defined in the provided code snippet._

- **[npc_dialogue]** SplitParts is not defined in the provided code snippet. This could lead to unexpected behavior or compilation errors.
  - Code: `string[] parts = SplitParts(content);`
  - Fix: Define the SplitParts method or ensure it's available in the current scope.
- **[npc_dialogue]** TrySplitKeyValue is not defined in the provided code snippet. This could lead to unexpected behavior or compilation errors.
  - Code: `if (TrySplitKeyValue(part, out string key, out string value))`
  - Fix: Define the TrySplitKeyValue method or ensure it's available in the current scope.
- **[npc_dialogue]** TryResolveDefinition is not defined in the provided code snippet. This could lead to unexpected behavior or compilation errors.
  - Code: `if (TryResolveDefinition(catalog, tagName, out var definition))`
  - Fix: Define the TryResolveDefinition method or ensure it's available in the current scope.

### `DevProject\Assets\Network_Game\Dialogue\MCP\MCPShowcasePolishController.cs`
_The MCPShowcasePolishController has potential multiplayer safety issues with non-Netcode-compliant fields and possible performance overhead from frequent EnsureSetup() calls. Review serialized fields to ensure they are NetworkObjects or accessed only after OnNetworkSpawn(). Optimize EnsureSetup() usage in frequently called methods._

- **[unity_best_practices]** EnsureSetup() is called inside GetAvailableShotIds(), which may be invoked frequently. This could lead to unnecessary overhead if EnsureSetup() performs heavy operations.
  - Location: `public string[] GetAvailableShotIds() { EnsureSetup(); ... }`
  - Code: `public string[] GetAvailableShotIds()
        {
            EnsureSetup();
            if (m_Shots == null || m_Shots.Count == 0)
            {
                return Array.Empty<string>();
            }

            var results = new List<string>(m_Shots.Count);
            for (int i = 0; i < m_Shots.Count; i++)
            {
                ShotBinding shot = m_Shots[i];
                if (shot == null || string.IsNullOrWhiteSpace(shot.ShotId))
                {
                    continue;
                }

                results.Add(shot.ShotId.Trim());
            }

            return results.ToArray();
        }`
  - Fix: Consider caching the result of EnsureSetup() or moving setup logic to a method that's only called once, reducing redundant checks in frequent calls.
- **[unity_best_practices]** Using a serialized list of ShotBinding objects may lead to performance issues if the list is large or frequently modified, as Unity's serialization can be inefficient with complex types.
  - Location: `[SerializeField] private List<ShotBinding> m_Shots = new List<ShotBinding>();`
  - Code: `[SerializeField]
        private List<ShotBinding> m_Shots = new List<ShotBinding>();`
  - Fix: Consider using a simpler data structure for shot bindings if performance becomes an issue. Alternatively, use a scriptable object for more efficient serialization and management.

### `DevProject\Assets\Network_Game\UI\Dialogue\ModernDialogueController.cs`
_The ModernDialogueController has potential multiplayer safety, ML-Agents training efficiency, Unity best practices, and NPC dialogue pipeline issues. Key fixes include ensuring network object access is delayed until OnNetworkSpawn(), increasing proximity check intervals for better performance, adding null checks for Transform fields, and verifying effect presence before accessing them._

- **[ml_agents]** The proximity check interval is set to a very low value, which may cause unnecessary computation and affect training performance. Consider increasing the interval for better efficiency.
  - Location: `private float m_MaxListenerDistance = 4f;`
  - Code: `private float m_ProximityCheckIntervalSeconds = 0.05f;`
  - Fix: Increase m_ProximityCheckIntervalSeconds to reduce frequent checks and improve training stability.
- **[unity_best_practices]** The Transform field is accessed without a null check. Ensure that the transform is properly initialized before use.
  - Location: `private Transform m_LocalPlayerTransform;`
  - Code: `private Transform m_LocalPlayerTransform;`
  - Fix: Add a null check for m_LocalPlayerTransform before accessing it in methods like Update().
- **[npc_dialogue]** The selected NPC is referenced as a NetworkObject without checking if it has an effect. Ensure to use EffectCatalog.HasEffect() before accessing any effects.
  - Location: `private NetworkObject m_SelectedNpc;`
  - Code: `private NetworkObject m_SelectedNpc;`
  - Fix: Check for HasEffect on the selected NPC before attempting to access or modify its effects.

### `DevProject\Assets\Network_Game\UI\Login\PlayerLoginController.cs`
_The PlayerLoginController.cs file has multiple instances where Object.FindObjectsByType is used inside methods that are called frequently (OnEnable, Update). This can lead to performance issues. The repeated use of FindObjectsByType in these contexts should be avoided by caching references or using static lists._

- **[unity_best_practices]** Calling FindObjectsByType inside Update or OnEnable can cause performance issues. Consider caching the references or using a static list.
  - Location: `OnEnable() uses Object.FindObjectsByType<>() inside method body.`
  - Code: `var inputs = Object.FindObjectsByType<Network_Game.ThirdPersonController.StarterAssetsInputs>(FindObjectsInactive.Include);`
  - Fix: Cache the input objects during initialization or use a static reference to avoid repeated calls to Object.FindObjectsByType<>().

## LOW Severity Issues

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSandboxRunner.cs`
_The EffectSandboxRunner.cs file has several issues related to maintainability, performance, and potential ML-Agent integration problems. These include hardcoded strings for effect tags, frequent calls to GetComponentsInChildren, and lack of explicit 'done' flag setting for ML-Agents during object destruction._

- **[npc_dialogue]** Hardcoded string used for effect tag parsing instead of using DialogueConstants.EffectTags. This makes the code less maintainable and harder to track which tags are valid.
  - Location: `public static GameObject PreviewEffect(...)`
  - Code: `var catalog = EffectCatalog.Load();`
  - Fix: Replace hardcoded strings with constants from DialogueConstants or similar centralized definition files.
- **[unity_best_practices]** GetComponentsInChildren is used inside a method that may be called frequently. This can cause performance issues if not cached or optimized.
  - Location: `private static void ApplyParameters(...)`
  - Code: `var systems = go.GetComponentsInChildren<ParticleSystem>();`
  - Fix: Cache the result of GetComponentsInChildren in a variable to avoid repeated calls.
- **[ml_agents]** The code does not explicitly set the 'done' flag for ML-Agents when destroying objects. This may lead to incorrect reward calculations or state tracking.
  - Location: `public static int RunLocalTest(...)`
  - Code: `Object.Destroy(instance, lifetime);`
  - Fix: Ensure that any object destruction is accompanied by a call to SetDone() on the corresponding agent if it's part of an ML-Agent training session.

### `DevProject\Assets\Network_Game\UI\Profile\PlayerProfileController.cs`
_The PlayerProfileController has potential performance issues due to frequent calls to RefreshClientIdLabel() within Update(), which could impact frame rate. Optimizing this method and reducing its call frequency would improve overall application efficiency._

- **[unity_best_practices]** The Update() method calls RefreshClientIdLabel(), which could be performance-intensive if not optimized. Consider using a coroutine with yield return or a flag to limit how often this is called.
  - Location: `private void Update() { RefreshClientIdLabel(); }`
  - Code: `private void Update()
{
    RefreshClientIdLabel();
}`
  - Fix: Replace the direct call in Update() with a flag that checks for changes before updating, or use a coroutine with yield return new WaitForSeconds() to throttle updates.
- **[unity_best_practices]** The RefreshClientIdLabel() method repeatedly checks for null and performs multiple conditional evaluations. Consider caching the results of these conditions to avoid redundant computations.
  - Location: `private void RefreshClientIdLabel() { ... }`
  - Code: `private void RefreshClientIdLabel()
{
    if (m_ClientIdLabel == null)
    {
        return;
    }

    string value = "-";
    if (
        NetworkManager.Singleton != null
        && NetworkManager.Singleton.IsListening
        && NetworkManager.Singleton.LocalClient != null
    )
    {
        value = NetworkManager.Singleton.LocalClientId.ToString();
    }
    else if (
        LocalPlayerAuthService.Instance != null
        && LocalPlayerAuthService.Instance.HasCurrentPlayer
    )
    {
        value = LocalPlayerAuthService.Instance.CurrentPlayer.PlayerId.ToString();
    }

    if (!string.Equals(m_ClientIdLabel.text, value))
    {
        m_ClientIdLabel.text = value;
    }
}`
  - Fix: Cache the result of NetworkManager.Singleton != null && ... in a variable before using it, reducing repeated condition checks.

## Clean Files (no issues detected)

- `DevProject\Assets\Network_Game\Combat\CombatHealthRegistry.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueBackendConfig.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueConstants.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueHistoryEntry.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueInferenceTypes.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueSemanticTag.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\EffectDefinition.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\EffectIntent.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSurface.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\ParticleParameterExtractor.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\SpatialResolverConstants.cs`
- `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueSpawnedMarker.cs`
- `DevProject\Assets\Network_Game\Dialogue\NpcDialogueProfile.cs`
- `DevProject\Assets\Network_Game\UI\ModernHudLayoutManager.cs`
- `DevProject\Assets\Network_Game\UI\ModernHudLayoutProfile.cs`
