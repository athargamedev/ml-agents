# Code Scan Report — 2026-03-04 02:01

**Files scanned**: 46  
**Total issues**: 43  
**High**: 18 | **Medium**: 19 | **Low**: 6  
**Duration**: 1635s

## HIGH Severity Issues

### `DevProject\Assets\Network_Game\Combat\CombatHealth.cs`
_The CombatHealth class has several potential issues related to multiplayer safety, Unity best practices, and ML-Agents observation normalization. The most critical issue is accessing NetworkObject before OnNetworkSpawn(), which can lead to desynchronization in a networked environment._

- **[multiplayer_safety]** Accessing NetworkObject in Awake() may be called before the object is spawned on the network, leading to potential null references or incorrect state synchronization. Use OnNetworkSpawn() for initialization that depends on network state.
  - Location: `Awake() method accesses NetworkObject before OnNetworkSpawn()`
  - Code: `m_CachedNetworkObject = GetComponent<NetworkObject>();`
  - Fix: Move m_CachedNetworkObject assignment to OnNetworkSpawn() instead of Awake().
- **[multiplayer_safety]** Setting NetworkVariable.Value in Awake() without checking if the object is owned by the server can lead to desynchronization. Ensure that any writes to NetworkVariables are guarded with IsServer/IsOwner checks.
  - Location: `Awake() sets CurrentHealth without server guard`
  - Code: `if (NetworkManager.Singleton == null || !NetworkManager.Singleton.IsListening) { m_CurrentHealth.Value = MaxHealth; }`
  - Fix: Guard the assignment of m_CurrentHealth.Value with a check for IsServer or IsOwner before setting it in Awake().

### `DevProject\Assets\Network_Game\Combat\CombatRuntimeOverlay.cs`
_The CombatRuntimeOverlay.cs file has potential multiplayer safety and Unity best practice issues, including a missing SyncVar for toggle key, improper singleton initialization, Awake usage for layout migration, and possible performance impact from frequent UI updates. These can be resolved by implementing proper synchronization mechanisms and optimizing the update logic._

- **[multiplayer_safety]** The toggle key is serialized but not protected against network desynchronization. Since this value can be modified by clients, it should use [SyncVar] or [ClientRpc] to ensure consistency across the network.
  - Location: `[SerializeField] private Key m_ToggleKey = Key.F9;`
  - Code: `[SerializeField]
private Key m_ToggleKey = Key.F9;`
  - Fix: Replace [SerializeField] with [SyncVar] for m_ToggleKey if server-authoritative, or add a ClientRpc method to sync changes when toggled.

### `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationContextBuilder.cs`
_The file contains potential multiplayer safety issues related to NetworkObject access and ML-Agents observation accuracy. Additionally, there are Unity best practices opportunities for optimization._

- **[multiplayer_safety]** Accessing NetworkObject via GetComponent/GetComponentInParent before OnEnable() can lead to incorrect references. Ensure network object is properly initialized by checking IsSpawned or using OnNetworkSpawn for critical operations.
  - Code: `m_TargetNpc = GetComponent<NetworkObject>();
            if (m_TargetNpc == null)
            {
                m_TargetNpc = GetComponentInParent<NetworkObject>();
            }`
  - Fix: Move ResolveTargetNpc() call to OnEnable() and ensure any access to m_TargetNpc uses IsSpawned check before use.

### `DevProject\Assets\Network_Game\Dialogue\DialogueClientUI.cs`
_The DialogueClientUI class has potential multiplayer safety issues due to early access of NetworkObject fields, a risk of rich-text injection via TextMeshPro, and possible unnecessary UI updates. These can be mitigated by ensuring networked references are accessed properly, sanitizing input, and adding change guards for UI elements._

- **[multiplayer_safety]** Accessing a NetworkObject field before OnNetworkSpawn() can lead to desynchronization. Ensure all NetworkObject references are accessed only after OnNetworkSpawn().
  - Location: `[SerializeField] private NetworkObject m_Speaker;`
  - Code: `[SerializeField]
        private NetworkObject m_Speaker;`
  - Fix: Move access of m_Speaker and m_Listener to OnNetworkSpawn() or ensure they are initialized properly in the networked context.
- **[multiplayer_safety]** Accessing a NetworkObject field before OnNetworkSpawn() can lead to desynchronization. Ensure all NetworkObject references are accessed only after OnNetworkSpawn().
  - Location: `[SerializeField] private NetworkObject m_Listener;`
  - Code: `[SerializeField]
        private NetworkObject m_Listener;`
  - Fix: Move access of m_Speaker and m_Listener to OnNetworkSpawn() or ensure they are initialized properly in the networked context.
- **[ml_agents]** Setting TextMeshPro.text directly without sanitization can introduce rich-text tags which may cause issues in WebGL builds. Use a sanitized version of the input.
  - Location: `m_InputField.text = prompt;`
  - Code: `if (string.IsNullOrEmpty(prompt))
                    continue;

                // Sanitize input to prevent rich text injection in WebGL builds.
                string sanitizedPrompt = Regex.Replace(prompt, "<[^>]+>", "");

                m_InputField.text = prompt;`
  - Fix: Use the sanitizedPrompt variable instead of raw prompt when setting m_InputField.text.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectProjectile.cs`
_The file contains potential multiplayer safety issues related to accessing network state without proper initialization and server-side guards, as well as some Unity best practices opportunities for optimization._

- **[multiplayer_safety]** Accessing NetworkObject without checking if the object is spawned. This can lead to desynchronization issues or invalid state access on clients.
  - Code: `if (networkManager.SpawnManager.SpawnedObjects.TryGetValue(m_SourceNetworkObjectId, out NetworkObject sourceObject) && sourceObject != null)`
  - Fix: Add a check for IsServer before accessing SpawnedObjects and ensure that any modifications to network variables are guarded with IsServer/IsOwner checks.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueFeedbackCollector.cs`
_The Awake() method may access NetworkDialogueService.Instance too early, which could lead to race conditions or null references. Move initialization code that depends on this service to OnEnable() or later._

- **[multiplayer_safety]** The Awake() method accesses the NetworkDialogueService.Instance in its logic, which might not be fully initialized at this stage. This could lead to race conditions or null reference exceptions when trying to access services that are only available after OnEnable().
  - Location: `Awake() is called before the scene is fully loaded and NetworkDialogueService may not be initialized yet.`
  - Code: `// Resolve output path relative to project root
m_ResolvedOutputPath = ResolveOutputPath(m_OutputPath);`
  - Fix: Move any code that depends on NetworkDialogueService.Instance to OnEnable() or later, ensuring the service is properly initialized before accessing it.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueParticleCollisionDamage.cs`
_The DialogueParticleCollisionDamage class applies combat damage without verifying if the target NetworkObject has authority. This can lead to desynchronization issues in multiplayer environments._

- **[multiplayer_safety]** The ApplyDamage method is called on a CombatHealth component without checking if the target NetworkObject has authority. This can lead to desynchronization issues in multiplayer environments.
  - Code: `.ApplyDamage(m_DamagePerHit, m_SourceNetworkObjectId, m_DamageType);`
  - Fix: Add a check for HasServerAuthority() before calling ApplyDamage, or ensure that only server-authoritative components handle damage application.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectIntent.cs`
_The EffectIntent class contains potential security risks and inconsistencies with the EffectCatalog system due to hardcoded tag names. These issues should be addressed to ensure safe and consistent NPC dialogue processing._

- **[npc_dialogue]** Hardcoded effect tag name as a literal instead of using DialogueConstants.* for consistency with the EffectCatalog system.
  - Location: `public string rawTagName;`
  - Code: `public string rawTagName;`
  - Fix: Replace hardcoded tag names with values from DialogueConstants class to ensure alignment with EffectCatalog definitions.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectParser.cs`
_The EffectParser.cs file contains several issues related to NPC dialogue handling, ML-Agents training stability, and Unity best practices. The primary concerns are the direct use of LLM response text in TextMeshPro without sanitization, lack of clamping on reward values, and potential performance issues from string operations._

- **[npc_dialogue]** LLM response text set directly into TextMeshPro.text without sanitising rich-text tags (WebGL risk)
  - Code: `[EFFECT: Lightning | at Player]`
  - Fix: Sanitize LLM response text by removing or escaping HTML/XML tags before setting TextMeshPro.text
- **[ml_agents]** AddReward() value not clamped — single large spike will destabilise PPO gradient
  - Code: `intent.scale = definition.defaultScale;`
  - Fix: Clamp the scale, duration, and color values before using them in ML-Agents observations

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSurface.cs`
_The file contains potential multiplayer safety issues due to direct access to child components without proper network object validation._

- **[multiplayer_safety]** GetComponentInChildren<Renderer>() is called without checking if the GameObject is a NetworkObject. This can lead to incorrect behavior in multiplayer scenarios where the object may not be properly synchronized.
  - Location: `public Renderer GetPrimaryRenderer() { ... }`
  - Code: `return GetComponentInChildren<Renderer>(includeInactive: true);`
  - Fix: Add a check for NetworkObject component or use GetComponent<NetworkObject>() and ensure the object is spawned before accessing child components.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectTargetResolverService.cs`
_The file contains a potential multiplayer safety issue where the NetworkObject component is accessed without proper ownership or spawn guards._

- **[multiplayer_safety]** Accessing NetworkObject component without OnNetworkSpawn guard. This can lead to desynchronization or invalid state on clients.
  - Code: `NetworkObject no = health.CachedNetworkObject != null ? health.CachedNetworkObject : health.GetComponent<NetworkObject>();`
  - Fix: Ensure access to NetworkObject is guarded with IsOwner || IsServer checks, and only accessed after OnNetworkSpawn()

### `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueMCPBridge.cs`
_The DialogueMCPBridge.cs file has potential multiplayer safety issues, ML-Agents compatibility concerns, and Unity best practices opportunities. The null check for NetworkDialogueService.Instance is missing in GetHistory(), which could lead to errors if the service isn't initialized yet. Additionally, there's a lack of observation normalization and possible UI string sanitization that may affect training stability and WebGL performance._

- **[multiplayer_safety]** Null check missing for NetworkDialogueService.Instance in GetHistory() — this instance is a scene singleton and may not be initialized yet if called during Awake().
  - Code: `if (service == null)`
  - Fix: Add null check for NetworkDialogueService.Instance before accessing its methods.

### `DevProject\Assets\Network_Game\Dialogue\NpcDialogueAnimationController.cs`
_The NpcDialogueAnimationController has several multiplayer safety and performance issues. The Animator setup is performed in Awake(), which may execute before network spawn, leading to inconsistent behavior on clients. Additionally, animation timing checks are not synchronized across clients, affecting ML-Agents training stability._

- **[multiplayer_safety]** GetComponentInChildren<Animator>() is called in Awake(), which runs before NetworkObject.OnNetworkSpawn(). This can lead to incorrect animator setup on clients, as the NetworkObject may not be spawned yet.
  - Location: `private void ResolveAnimator() { ... }`
  - Code: `if (m_Animator == null) { m_Animator = GetComponentInChildren<Animator>(true); }`
  - Fix: Move ResolveAnimator() call to OnNetworkSpawn() or ensure it's only executed after network spawn via a flag.
- **[multiplayer_safety]** CacheParameters() accesses Animator.parameters without checking if the animator is properly initialized. This can cause issues on clients where the animator may not be ready.
  - Location: `private void CacheParameters() { ... }`
  - Code: `if (m_Animator == null || m_Animator.parameters == null) { return; }`
  - Fix: Add a check for IsServer or use OnNetworkSpawn() to ensure the animator is available before accessing its parameters.

## MEDIUM Severity Issues

### `DevProject\Assets\Network_Game\Combat\CombatHealth.cs`
_The CombatHealth class has several potential issues related to multiplayer safety, Unity best practices, and ML-Agents observation normalization. The most critical issue is accessing NetworkObject before OnNetworkSpawn(), which can lead to desynchronization in a networked environment._

- **[ml_agents]** The NormalizedHealth property uses a direct division without applying the required denominator normalization. This may lead to inconsistent observations for ML-Agents training.
  - Location: `NormalizedHealth is not clamped or normalized as per observation space rules`
  - Code: `public float NormalizedHealth => Mathf.Clamp01(CurrentHealth / MaxHealth);`
  - Fix: Ensure that all observation values are divided by a constant denominator before being used in sensor.AddObservation(). For example, use (CurrentHealth / MaxHealth) / 10f as per your observation space scaling rules.

### `DevProject\Assets\Network_Game\Combat\CombatRuntimeOverlay.cs`
_The CombatRuntimeOverlay.cs file has potential multiplayer safety and Unity best practice issues, including a missing SyncVar for toggle key, improper singleton initialization, Awake usage for layout migration, and possible performance impact from frequent UI updates. These can be resolved by implementing proper synchronization mechanisms and optimizing the update logic._

- **[unity_best_practices]** The EnsureInstance method uses FindAnyObjectByType with FindObjectsInactive.Include, which can lead to unexpected behavior if multiple instances exist or during scene transitions. Consider using a static instance variable and singleton pattern for better control.
  - Location: `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.AfterSceneLoad)] private static void EnsureInstance() { ... }`
  - Code: `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.AfterSceneLoad)]
private static void EnsureInstance()
{
#if UNITY_2023_1_OR_NEWER
    CombatRuntimeOverlay existing = FindAnyObjectByType<CombatRuntimeOverlay>(FindObjectsInactive.Include);
#else
    CombatRuntimeOverlay existing = FindAnyObjectByType<CombatRuntimeOverlay>(FindObjectsInactive.Include);
#endif
    if (existing != null)
    {
        return;
    }

    GameObject overlayRoot = new GameObject("CombatRuntimeOverlay");
    DontDestroyOnLoad(overlayRoot);
    overlayRoot.AddComponent<CombatRuntimeOverlay>();
}`
  - Fix: Replace the RuntimeInitializeOnLoadMethod with a proper singleton pattern (e.g., static Instance property) to ensure single instance creation and avoid FindAnyObjectByType usage.
- **[unity_best_practices]** Awake is called before the object is added to the scene, which may lead to issues with UI Toolkit or other systems that require the object to be active. Consider moving initialization logic to OnEnable or a later stage.
  - Location: `private void Awake() { ApplyCompactLayoutMigration(); }`
  - Code: `private void Awake()
{
    ApplyCompactLayoutMigration();
}`
  - Fix: Move ApplyCompactLayoutMigration() call from Awake() to OnEnable() or another method called after the object is added to the scene.
- **[unity_best_practices]** The Update method contains a potential typo (m_Show instead of m_ShowOverlay) and performs frequent UI updates which can impact performance. Consider adding change guards or optimizing the refresh logic.
  - Location: `private void Update() { ... }`
  - Code: `private void Update()
{
    TryEnsureUiToolkitOverlay();

    if (Keyboard.current != null && Keyboard.current[m_ToggleKey].wasPressedThisFrame)
    {
        m_ShowOverlay = !m_Show; // Note: typo here, should be m_ShowOverlay
    }

    if (!m_ShowOverlay)
    {
        return;
    }

    if (m_PlayerListDirty)
    {
        RebuildPlayerHealthTargets();
    }

    float now = Time.unscaledTime;
    PruneExpiredLogs(m_RecentEffects, now);
    PruneExpiredLogs(m_RecentDamage, now);
    RefreshUiToolkitOverlay();
}`
  - Fix: Correct the typo from 'm_Show' to 'm_ShowOverlay'. Optimize RefreshUiToolkitOverlay() by using change guards or limiting update frequency.

### `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationContextBuilder.cs`
_The file contains potential multiplayer safety issues related to NetworkObject access and ML-Agents observation accuracy. Additionally, there are Unity best practices opportunities for optimization._

- **[unity_best_practices]** Awake() is not guaranteed to be called after NetworkObject initialization. Move network-related setup to OnEnable() or later.
  - Code: `private void Awake()
        {
            ResolveTargetNpc();
        }`
  - Fix: Move ResolveTargetNpc() call from Awake() to OnEnable() for proper network object initialization.
- **[ml_agents]** The 'IsSpeaking' flag is derived from a time-based threshold without considering the actual dialogue content or speech duration. This may lead to inaccurate observations for ML-Agents training.
  - Code: `live.IsSpeaking = age <= Mathf.Max(0.05f, m_SpeakingWindowSeconds);`
  - Fix: Consider incorporating additional features like word count or sentence length into the IsSpeaking determination for more accurate state representation.
- **[unity_best_practices]** Repeated calls to GetComponent/GetComponentInParent inside ResolveTargetNpc() can be inefficient. Cache the result or use a single call with fallback.
  - Code: `private void ResolveTargetNpc()
        {
            if (m_TargetNpc != null)
            {
                return;
            }

            m_TargetNpc = GetComponent<NetworkObject>();
            if (m_TargetNpc == null)
            {
                m_TargetNpc = GetComponentInParent<NetworkObject>();
            }`
  - Fix: Optimize by using a single GetComponent call and checking for null before falling back to parent components.

### `DevProject\Assets\Network_Game\Dialogue\DialogueClientUI.cs`
_The DialogueClientUI class has potential multiplayer safety issues due to early access of NetworkObject fields, a risk of rich-text injection via TextMeshPro, and possible unnecessary UI updates. These can be mitigated by ensuring networked references are accessed properly, sanitizing input, and adding change guards for UI elements._

- **[unity_best_practices]** Updating TextMeshPro.text in a loop without checking for changes can cause unnecessary UI updates. Consider adding a change guard.
  - Location: `m_OutputText.text = formattedOutput;`
  - Code: `if (string.IsNullOrEmpty(formattedOutput))
                    continue;

                // Format output with rich text and line breaks.
                string formattedOutput = "";

                m_OutputText.text = formattedOutput;`
  - Fix: Add a check to ensure the text is only updated when it actually changes.

### `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectProjectile.cs`
_The file contains potential multiplayer safety issues related to accessing network state without proper initialization and server-side guards, as well as some Unity best practices opportunities for optimization._

- **[unity_best_practices]** Using a fixed-size array for overlap buffer may lead to inefficiency if the number of overlapping objects exceeds the buffer size. Consider using a dynamic list or increasing the buffer size appropriately.
  - Code: `private Collider[] m_OverlapBuffer = new Collider[DefaultOverlapBufferSize];`
  - Fix: Use a List<Collider> instead of an array for m_OverlapBuffer, or increase the default buffer size based on expected usage.
- **[ml_agents]** Clamping values is good practice, but ensure that the clamped value does not introduce unintended behavior in reward shaping or observation normalization. Verify if this clamping aligns with your PPO training requirements.
  - Code: `m_DamageAmount = Mathf.Max(0f, damageAmount);`
  - Fix: Review the clamping logic to confirm it's consistent with your ML-Agents reward and observation scaling strategies.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectParser.cs`
_The EffectParser.cs file contains several issues related to NPC dialogue handling, ML-Agents training stability, and Unity best practices. The primary concerns are the direct use of LLM response text in TextMeshPro without sanitization, lack of clamping on reward values, and potential performance issues from string operations._

- **[unity_best_practices]** String interpolation / new string inside Update() updating UI every frame without a change-guard
  - Code: `string[] parts = SplitParts(content);`
  - Fix: Avoid creating new strings in performance-critical paths; use StringBuilder for dynamic content creation

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSandboxRunner.cs`
_The file contains potential issues related to effect catalog usage, performance optimization of particle systems, and efficient transform operations. These are categorized as medium severity due to the risk of runtime errors or inefficiencies during local testing._

- **[npc_dialogue]** EffectCatalog is registered at runtime via NpcDialogueActor; always check HasEffect before lookup
  - Location: `public static GameObject PreviewEffect(...)`
  - Code: `var catalog = EffectCatalog.Load();`
  - Fix: Check if EffectCatalog has the effect before attempting to load it.

### `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueMCPBridge.cs`
_The DialogueMCPBridge.cs file has potential multiplayer safety issues, ML-Agents compatibility concerns, and Unity best practices opportunities. The null check for NetworkDialogueService.Instance is missing in GetHistory(), which could lead to errors if the service isn't initialized yet. Additionally, there's a lack of observation normalization and possible UI string sanitization that may affect training stability and WebGL performance._

- **[ml_agents]** No evidence of observation normalization in GetProfiles() — if this data is used as observations, raw values may destabilize PPO training.
  - Code: `return new Dictionary<string, object> { ["id"] = p.ProfileId, ... }`
  - Fix: Normalize profile IDs or keywords before adding to observations.
- **[unity_best_practices]** FindObjectOfType / FindObjectsByType is not used here, but GetAvailableEffects() may have performance implications if called frequently.
  - Code: `return DialogueSceneEffectsController.GetAvailableEffects();`
  - Fix: Cache the result of GetAvailableEffects() and reuse it instead of calling repeatedly.

### `DevProject\Assets\Network_Game\Dialogue\NpcDialogueAnimationController.cs`
_The NpcDialogueAnimationController has several multiplayer safety and performance issues. The Animator setup is performed in Awake(), which may execute before network spawn, leading to inconsistent behavior on clients. Additionally, animation timing checks are not synchronized across clients, affecting ML-Agents training stability._

- **[ml_agents]** The IsReadyForAction check uses Time.time which is not synchronized across clients. This can lead to inconsistent behavior in ML-Agents training.
  - Location: `public bool TryPlayAction(DialogueAnimationAction action, out string reason)`
  - Code: `if (action != DialogueAnimationAction.HoldNeutral && !IsReadyForAction) { ... }`
  - Fix: Use a NetworkVariable or synchronize the action timing via ServerRpc for consistent state across all agents.

### `DevProject\Assets\Network_Game\UI\Login\PlayerLoginController.cs`
_The PlayerLoginController uses Object.FindObjectsByType in methods that are called frequently (ApplyUiCursorAndLookState, RestoreGameplayCursorAndLookState), which can be inefficient. Consider caching references or using a static list to avoid repeated searches._

- **[unity_best_practices]** Calling FindObjectsByType in ApplyUiCursorAndLookState and RestoreGameplayCursorAndLookState can be inefficient. Consider caching the references or using a static list to avoid repeated searches.
  - Location: `OnEnable() calls Object.FindObjectsByType<>() inside Update() or OnActionReceived() — must see the call inside the method body`
  - Code: `var inputs = Object.FindObjectsByType<Network_Game.ThirdPersonController.StarterAssetsInputs>(FindObjectsInactive.Include);`
  - Fix: Cache the inputs reference during initialization or use a static list of input components to reduce redundant calls to Object.FindObjectsByType<>().

### `DevProject\Assets\Network_Game\UI\Profile\PlayerProfileController.cs`
_The PlayerProfileController has several Unity best practices issues related to frequent UI updates, redundant checks, and potential inefficiencies in the Update() method. These can be optimized by adding change-guards, refactoring repetitive code, and simplifying conditional logic._

- **[unity_best_practices]** The Update() method calls RefreshClientIdLabel(), which may perform frequent UI updates. Consider adding a change-guard to avoid unnecessary updates on every frame.
  - Location: `private void Update() { RefreshClientIdLabel(); }`
  - Code: `private void Update()
{
    RefreshClientIdLabel();
}`
  - Fix: Add a flag to track if the client ID has changed before calling RefreshClientIdLabel().
- **[unity_best_practices]** The method directly modifies UI elements (e.g., Label text) without checking if the value has changed. This can cause unnecessary UI updates on every call.
  - Location: `private void UpdateProfile(LocalPlayerAuthService.LocalPlayerRecord record)`
  - Code: `private void UpdateProfile(LocalPlayerAuthService.LocalPlayerRecord record)
{
    m_NameLabel.text = record.NameId.ToUpper();
    ...`
  - Fix: Compare the new value with the current one before updating the UI to avoid redundant changes.
- **[unity_best_practices]** The method contains multiple conditional checks for client ID retrieval. Consider refactoring to reduce redundancy and improve readability.
  - Location: `private void RefreshClientIdLabel() { ... }`
  - Code: `private void RefreshClientIdLabel()
{
    if (m_ClientIdLabel == null)
    {
        return;
    }

    string value = "-";`
  - Fix: Refactor the logic into separate helper methods or use a more streamlined approach for retrieving the client ID.

## LOW Severity Issues

### `DevProject\Assets\Network_Game\Dialogue\DialogueDebugPanel.cs`
_The file contains potential performance optimizations and memory management improvements. The issues reported are not critical but could impact long-term stability or efficiency, especially under heavy load or over extended periods of runtime._

- **[unity_best_practices]** Potential performance issue: Using a float for time tracking may lead to precision loss over long periods. Consider using double or Time.timeSinceLevelLoad instead.
  - Code: `private float m_NextRefreshTime;`
  - Fix: Replace m_NextRefreshTime with Time.timeSinceLevelLoad or use a double type for higher precision.
- **[unity_best_practices]** Potential performance issue: Using a List<T> for caching may cause frequent memory allocations. Consider using a fixed-size buffer or pooling for better efficiency.
  - Code: `private System.Collections.Generic.List<Network_Game.Dialogue.MCP.DialogueMCPBridge.LMStudioLogEntry> m_CachedLMLog;`
  - Fix: Replace the list with a pooled object or use a fixed-size array for LMStudio log entries.
- **[unity_best_practices]** Potential performance issue: Using a List of Dictionaries may lead to high memory overhead and slower access times. Consider using more efficient data structures for VFX state tracking.
  - Code: `private System.Collections.Generic.List<System.Collections.Generic.Dictionary< string, object >> m_CachedVfxState;`
  - Fix: Replace with a more efficient structure like a Dictionary<string, object[]> or use pooling for frequent allocations.
- **[unity_best_practices]** Potential memory leak risk: UI elements are not explicitly destroyed on disable. Ensure proper cleanup in OnDisable() to prevent memory leaks.
  - Code: `private VisualElement m_UiHostRoot;`
  - Fix: Add code to remove or destroy the UiToolkit overlay in OnDisable() to ensure resources are freed.

### `DevProject\Assets\Network_Game\Dialogue\Effects\EffectSandboxRunner.cs`
_The file contains potential issues related to effect catalog usage, performance optimization of particle systems, and efficient transform operations. These are categorized as medium severity due to the risk of runtime errors or inefficiencies during local testing._

- **[unity_best_practices]** GetComponentsInChildren is called inside a method that may be called frequently; consider caching or using FindObjectsOfType with a layer mask for performance.
  - Location: `private static void ApplyParameters(...)`
  - Code: `var systems = go.GetComponentsInChildren<ParticleSystem>();`
  - Fix: Use a cached reference to the ParticleSystem components or use FindObjectsOfType with appropriate layers for better performance in hot paths.
- **[unity_best_practices]** Transform operations can be expensive; consider using localScale directly with multiplication for better performance.
  - Location: `private static void ApplyParameters(...)`
  - Code: `go.transform.localScale = go.transform.localScale * scale;`
  - Fix: Use go.transform.localScale *= scale; to avoid redundant assignments and improve efficiency.

## Clean Files (no issues detected)

- `DevProject\Assets\Network_Game\Combat\CombatHealthRegistry.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationAction.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueAnimationDecisionPolicy.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueBackendConfig.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueConstants.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueHistoryEntry.cs`
- `DevProject\Assets\Network_Game\Dialogue\DialogueInferenceTypes.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueEffectSpatialResolver.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\DialogueSemanticTag.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\EffectDefinition.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\ParticleParameterExtractor.cs`
- `DevProject\Assets\Network_Game\Dialogue\Effects\SpatialResolverConstants.cs`
- `DevProject\Assets\Network_Game\Dialogue\MCP\DialogueSpawnedMarker.cs`
- `DevProject\Assets\Network_Game\Dialogue\NpcDialogueProfile.cs`
- `DevProject\Assets\Network_Game\UI\ModernHudLayoutManager.cs`
- `DevProject\Assets\Network_Game\UI\ModernHudLayoutProfile.cs`
